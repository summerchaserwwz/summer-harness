interface:
  display_name: "轻量 Harness 路由"
  short_description: "Choose the lightest sufficient workflow route"
  default_prompt: "Use $adaptive-harness-router to choose the lightest sufficient route."
policy:
  allow_implicit_invocation: false
